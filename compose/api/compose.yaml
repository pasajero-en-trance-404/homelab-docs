services:
  api:
    build: .
    container_name: api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    networks:
      - homelab

networks:
  homelab:
    external: true
