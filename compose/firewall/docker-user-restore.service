[Unit]
Description=Restore DOCKER-USER firewall rules after Docker
After=network.target docker.service tailscaled.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/restore-docker-user.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
